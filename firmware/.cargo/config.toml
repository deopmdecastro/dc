[build]
target = "xtensa-esp32s3-espidf"
target-dir = "../../t"

[env]
CARGO_WORKSPACE_DIR = { value = "", relative = true }
ESP_IDF_GLOB_PROJECT_BASE = { value = "", relative = true }
ESP_IDF_GLOB_PROJECT_PARTITIONS = "partitions.csv"

[target.xtensa-esp32s3-espidf]
linker = "ldproxy"

[unstable]
build-std = ["std", "panic_abort"]
