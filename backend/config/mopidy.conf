[core]
cache_dir = /var/lib/mopidy/cache
config_dir = /config
data_dir  = /var/lib/mopidy

[audio]
output = autoaudiosink

[http]
enabled  = true
hostname = 0.0.0.0
port     = 6680

[mpd]
enabled  = true
hostname = 0.0.0.0
port     = 6600

# Ative preenchendo com sua conta premium via env (SPOTIFY_USER / SPOTIFY_PASS).
[spotify]
enabled  = false
username =
password =
client_id =
client_secret =
